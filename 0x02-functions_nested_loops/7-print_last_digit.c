#include "main.h"

/**
 *print_last_digit - checks code
 *
 *@x: parameter value
 *
 *Return: Returns the value of the last digit x
 */

int print_last_digit(int x)
{

if (x >= 0)
{

int n = 0;

while (n < 2)
{
int y = x % 10;
int y2 = x % 10;

return (y);
return (y2);
}
}

else if (x < 0)
{

int n = 0;

while (n < 2)
{
int y = x % (-10);
int y2 = x % (-10);

return (y);
return (y2);
}
}

return (0);
}
