#include <stdio.h>

/**
 * main - main function
 *
 * Return: Always 0 (Succes)
 */

int main(void)
{
char new_string[] = "with proper grammar, but the outcome is a piece of art,";
printf("%s\n", new_string);
return (0);
}
